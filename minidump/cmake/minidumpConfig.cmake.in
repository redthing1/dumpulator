@PACKAGE_INIT@

# Find dependencies
# (none required - this is a self-contained library)

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/minidumpTargets.cmake")

# Provide the minidump::minidump target
check_required_components(minidump)